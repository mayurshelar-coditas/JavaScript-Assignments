//High Order function.

const addition = (firstOperand, secondOperand) => {
    return firstOperand + secondOperand;
} 

const map = ((array, callback) => {
        const modifiedArray = [];
        for(const number in numbersArray){
            modifiedArray[number] = numbersArray[number]**2;
        }
        return modifiedArray;
});



const squareArray = (displaySum([1,2,3,4,5]));
console.log(squareArray);

